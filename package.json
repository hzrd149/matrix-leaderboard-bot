{
  "name": "matrix-scoreboard-bot",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "postinstall": "./fix-node_modules.sh || true",
    "dev": "nodemon --experimental-specifier-resolution=node --watch src src/index.js",
    "format": "prettier -w . --ignore-path .gitignore",
    "start": "node --experimental-specifier-resolution=node src/index.js"
  },
  "dependencies": {
    "matrix-bot-starter": "^1.4.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "prettier": "^2.8.8"
  }
}
